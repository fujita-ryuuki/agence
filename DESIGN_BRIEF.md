# DESIGN_BRIEF — Agence Agent Builder PoC（Claude Design 用）

## プロダクト一言
専門家が、自分の資料と相談への回答を投入するだけで「自分の代わりに答える Agent」を育て、その到達度を確かめる Web アプリ。PoC のため、専門家（3〜5 名）・ブラインド評価者・運用者の 3 ロールのみ。

## ユーザーと利用文脈
- 専門家：40〜60 代の経営・法務・医療などの実務家。非エンジニア。PC のブラウザで、週に数回・1 回 30〜60 分の作業（資料投入、相談への回答、AI の整理結果の確認、Agent 回答の採点）。「自分の知識が正しく扱われているか」への不安が強い。
- ブラインド評価者：同領域の別専門家。A/B の回答を読み優劣を付けるだけ。
- 運用者：Maxeff の PdM。データが多い管理画面を使う。

## デザイン原則
1. 「AI が勝手に何かした」感を出さない。常に「どの資料から」「どの回答から」来た知識かが見える。
2. 専門家の作業は 4 種類だけ（入れる／答える／確かめる／採点する）。ナビも 4 動詞を軸に。
3. 技術用語（Vault、Markdown、RAG、埋め込み、chunk）を UI に出さない。「知識」「原則」「判断基準」「事例」「出典」で語る。
4. 長い処理は待たせない。進捗を見せ、離脱しても戻ってこられる。
5. 採点画面は集中できるように余白を多く、1 画面 1 相談。

## トーン & ビジュアル
- 落ち着いた専門職向け。信頼感・静かさ。装飾的なグラデーションやイラストは不要。
- ライトテーマ基調。ダーク対応は任意。
- カラー：ベースは白〜ごく薄いグレー、テキストはほぼ黒（#1a1a1a）、アクセント 1 色（深い青緑 例 #0F6E6E）＋状態色（成功 緑／注意 琥珀／失敗 赤／処理中 青）。
- タイポ：日本語は Noto Sans JP（または system-ui）、本文 15px、行間 1.7。数値は tabular-nums。
- 密度：専門家画面は中程度、管理画面は高密度（テーブル中心）。
- コンポーネント：shadcn/ui のスタイルに寄せる（Card, Tabs, Table, Badge, Dialog, Sheet, Toast, Progress, Slider, Radio）。
- 図表：折れ線・積み上げ棒のみ（Recharts 想定）。目標線（4.0 / 70%）を必ず描く。

## 生成してほしい画面（優先順）
docs/03_画面設計仕様.md の画面 ID に対応。
1. S01 ダッシュボード（4 カード＋レベル＋今週の作業時間、空状態版も）
2. S03 データ投入（形式タブ、Slack 接続後のチャンネル選択、投入一覧、失敗行）
3. S04a 疑似相談 回答（テキスト／音声タブ、フラグ、確定）
4. S05 知識を確認（層タブ、要確認フィルタ、確信度バッジ）＋ S05a ノート詳細（出典パネル、正しい／修正／削除）
5. S05b 矛盾・重複（左右並列、A/B/両方）
6. S06a 評価詳細（相談｜Batch 横並び回答｜根拠パネル、スコア 1〜5、減点タグ）
7. S07 レベル・推移（KPI 4 枚＋グラフ 4 枚）
8. S03a 文字起こし確認（セグメント編集＋プレイヤー）
9. R01a ブラインド比較（A/B 等幅、判定＋理由）
10. O03 Batch 管理（締めパイプラインの段階表示）、O04 ジョブ一覧
11. S00 ログイン、S02 専門領域、S08 設定

## 各画面に必ず含める状態
空状態／読込中／処理中（ジョブ進行）／失敗（理由＋再試行）／完了。破壊的操作は確認モーダル。

## 出力形式
- 画面ごとに 1 ファイル（React + Tailwind + shadcn/ui、TypeScript）。ダミーデータは `docs/04_データモデル.md` の型名に合わせる。
- 共通レイアウト（サイドナビ＋ヘッダー）を 1 つ、ロール別にナビ項目を切替。
- デザイントークン（色・余白・角丸・タイポ）を `tokens.css` として出力。
